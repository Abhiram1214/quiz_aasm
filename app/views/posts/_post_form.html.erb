 <%= form_for(@new_post) do |f| %>

        <%= f.label :user %>:
         <%= @email %><br><br>

     <%= f.label :post %>
     <%= f.text_area :text %><br><br>

<%= f.hidden_field :user_id, :value=>current_user.id %>

<%= f.submit %>

 <% end %>
